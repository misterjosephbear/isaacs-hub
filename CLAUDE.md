## Persistent Memory

This project's long-term memory lives in `~/vault/projects/isaacs-hub/`.

Before starting non-trivial work, check:
- `~/vault/Decisions/` for past architectural choices (search/grep for this project's name or relevant tags)
- `~/vault/Mistakes/` for known pitfalls in this codebase
- `~/vault/projects/isaacs-hub/architecture/` for how this project is structured
- `~/vault/projects/isaacs-hub/graphify/` (if present) for a queryable code structure graph — check it before reading raw source for "how does X connect to Y" questions

Always write new decisions and recurring bugs back to these folders (`~/vault/Decisions/`, `~/vault/Mistakes/`), not just into this file.

## Context Navigation

Before reading raw source files to answer a "how does X work" / "how does X connect to Y" question, check layers in this order — each is cheaper than the last:

1. `~/vault/projects/isaacs-hub/graphify/graph.json` or `~/vault/projects/isaacs-hub/graphify/wiki/index.md` — code structure and connections as a queryable graph, generated by [graphify](https://github.com/Graphify-Labs/graphify). Regenerate with `graphify extract . --code-only && graphify cluster-only . --no-label` from this repo, then `graphify export obsidian --graph graphify-out/graph.json --dir ~/vault/projects/isaacs-hub/graphify` (a git post-commit hook also keeps `graphify-out/graph.json` current automatically).
2. `~/vault/projects/isaacs-hub/` — decisions, progress, and context notes (see Persistent Memory above).
3. Raw source files — only if the first two layers don't answer the question.
